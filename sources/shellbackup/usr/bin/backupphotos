#!/bin/bash

echo backupphotos by ally && echo creating backup && cp -r /var/mobile/Media/DCIM /var/mobile/shellbackup && echo backup created at /var/mobile/shellbackup/DCIM && echo type restorephotos to restore your photos and or videos from this backup