#!/bin/bash

echo restoreall by ally && echo unzipping backup at /var/mobile/shellbackup.zip && cd /var/mobile && unzip shellbackup.zip && echo backup unzipped && echo deleting old backup zip file && rm /var/mobile/shellbackup.zip && echo old backup deleted && echo deleting all current contacts && rm -r /var/mobile/Library/AddressBook && echo restoring contacts backup from /var/mobile/shellbackup/AddressBook && cp -r /var/mobile/shellbackup/AddressBook /var/mobile/Library && echo setting ownership && chown mobile -R /var/mobile/Library/AddressBook && echo setting permissions && chmod -R 0755 /var/mobile/Library/AddressBook && echo contacts backup restored && echo deleting contacts backup && rm -r /var/mobile/shellbackup/AddressBook && echo deleting all current messages && rm -r /var/mobile/Library/SMS && echo restoring SMS or messages backup from /var/mobile/shellbackup/SMS && cp -r /var/mobile/shellbackup/SMS /var/mobile/Library && echo setting ownership && chown mobile -R /var/mobile/Library/SMS && echo setting permissions && chmod -R 0755 /var/mobile/Library/SMS && echo SMS or messages backup restored && echo deleting SMS or messages backup && rm -r /var/mobile/shellbackup/SMS && echo deleting all current photos and or videos && rm -r /var/mobile/Media/DCIM && echo deleting old photos database && rm /var/mobile/Media/PhotoData/Photos.sqlite && echo restoring backup from /var/mobile/shellbackup/DCIM && cp -r /var/mobile/shellbackup/DCIM /var/mobile/Media && echo setting ownership && chown mobile -R /var/mobile/Media/DCIM && echo setting permissions && chmod -R 0755 /var/mobile/Media/DCIM && echo old photos database deleted && echo backup restored && echo deleting backup && rm -r /var/mobile/shellbackup/DCIM && echo all data will be restored the next time you power on your device && echo SHUTDOWN YOUR DEVICE RIGHT NOW && echo SHUTOFF YOUR DEVICE RIGHT NOW && echo SHUTOFF YOUR DEVICE RIGHT NOW