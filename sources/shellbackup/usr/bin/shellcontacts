#!/bin/bash 

echo shellbackup by ally && echo shellcontacts usage && echo  Backing up and restoring only contacts && echo && echo The Contacts app cannot be running in the background when using the backupnotes or restorenotes commands. Type backupnotes as the root user to create a backup at /var/mobile/shellbackup/AddressBook of the on device data of the Contacts app. Every time you make a backup, it will overwrite the last one unless you move the old backup to somewhere else before running backupnotes. Type restorecontacts as the root user to restore the backup at /var/mobile/shellbackup/AddressBook. && echo && echo How to backup Contacts and restore them to a different device or firmware && echo && echo Backups are cross firmware and cross device compatible. You can backup contacts on any firmware or device and restore them to any other firmware or other device as long as both are supported by shellbackup. And have it installed. Simply copy the directory AddressBook from /var/mobile/shellbackup and paste it to the other device or firmware of the same device at /var/mobile/shellbackup. Some older versions of the contacts app don’t support newer features of the modern versions.  If you restore a backup to a lower firmware, some features might be missing. 